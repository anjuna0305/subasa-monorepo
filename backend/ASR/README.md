# Voc-Si - Speech-to-Speech Chatbot

## ASR Model
This guide will help you set up the ASR model for Voc-Si. Please ensure you have **Python 3.8** or a higher version installed.

---
To install the required packages, run the following command:

```bash
pip install huggingface_hub transformers torch sounddevice numpy

