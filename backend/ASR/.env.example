# Hugging Face token for pulling the model checkpoints.
HF_TOKEN=

# Explicit origins only, comma-separated. "*" is rejected at startup.
CORS_ALLOW_ORIGINS=http://localhost:7007,http://localhost:5173
